services:
  nginx:
    image: nginx:latest
    container_name: nginx_server
    ports:
      - "80:80" # Map port 80 on the host to port 80 in the container
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro # Custom configuration
      - ./html:/usr/share/nginx/html:ro       # Static website content
